# scitex-dev project config
# No project-type declared: scitex-storage is a general-purpose CLI tool,
# not a `research` (figure/provenance-gated) checkout — the default
# (non-research) rule set applies.
#
# NO SKIP-RULES. The PS-108b deferral that lived here from 2026-07-28 to
# 2026-08-11 is removed because the refactor it deferred has landed: the
# flat-module count went 23 -> 4 (threshold 15) when _transfer/ and _measure/
# were created, and the audit now reports "[PS-108b] 0 violation(s) masked
# (no current violations — entry is now removable)".
#
# Keeping it would be the exact debt the mechanism exists to surface: a skip
# that outlives its campaign. Its own rationale had been rewritten three times
# while the count it described moved 17 -> 19 -> 21 -> 23, and the "hard rule"
# it carried (no new modules at this root) was unenforceable precisely BECAUSE
# the rule was masked — a guard cannot fire through the exemption that hides it.
# That is why the count kept moving with nobody noticing.
